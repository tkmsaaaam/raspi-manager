apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector
  labels:
    app: otel-collector
data:
  config.yaml: |
    receivers:
      filelog/container:
        include:
          - /var/log/containers/*/*/*.log
        operators:
          - type: container
      filelog/clamav:
        include:
          - /var/log/clamav/clamav.log
          - /var/log/clamav/daily-scan.log
          - /var/log/clamav/freshclam.log
      prometheus:
        trim_metric_suffixes: true
        config:
          global:
            scrape_interval: 15s
            scrape_timeout: 10s
            scrape_protocols:
              - OpenMetricsText1.0.0
              - OpenMetricsText0.0.1
              - PrometheusText0.0.4
            evaluation_interval: 15s
          scrape_configs:
            - job_name: node
              static_configs:
                - targets:
                    - node-exporter-svc:9100
            - job_name: cadvisor
              static_configs:
                - targets:
                    - cadvisor-svc.cadvisor.svc.cluster.local:8080
    exporters:
      otlphttp/metrics:
        endpoint: "http://victoriametrics-svc:8428/opentelemetry/v1/metrics"
      otlphttp/logs:
        endpoint: "http://victorialogs-svc:9428/insert/opentelemetry/v1/logs"
    service:
      pipelines:
        metrics:
          receivers: [prometheus]
          exporters: [otlphttp/metrics]
        logs:
          receivers: [filelog/container, filelog/clamav]
          exporters: [otlphttp/logs]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
  labels:
    app: otel-collector
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-collector
  template:
    metadata:
      labels:
        app: otel-collector
    volumes:
        - name: logs
          hostPath:
            path: /var/log
            type: Directory
    spec:
      serviceAccountName: otel-collector
      containers:
        - name: otel-collector
          image: docker.io/otel/opentelemetry-collector-contrib:0.157.0 # https://github.com/open-telemetry/opentelemetry-collector-contrib/releases https://hub.docker.com/r/otel/opentelemetry-collector-contrib
          args: ['--config', '/etc/config/config.yaml']
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: logs
              mountPath: /var/log
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 512Mi
